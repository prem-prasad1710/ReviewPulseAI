/** Assumed aggregator commission for *estimation only* — user can override in UI. */
export const DEFAULT_PLATFORM_FEE_PCT: Record<string, number> = {
  UBER: 0.25,
  OLA: 0.22,
  RAPIDO: 0.18,
  OTHER: 0.22,
};
